#include <bits/stdc++.h>
#include <algorithm>
#include <string>
using namespace std;

#define ll long long
#define fo(i, n) for (int i = 0; i < n; i++)

int main() {
  ll int n, k, t, i, p, q, r, s, count = 0, j;

 
}

/*
 cin >> n;
    int arr[i];
    fo(i, n){
      cin >> arr[i];

      fo(i,n){
      if (arr[i]<arr[i+1])
            count=i;
      }
    }
    cout << endl;
*/