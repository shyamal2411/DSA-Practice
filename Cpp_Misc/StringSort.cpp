#include <bits/stdc++.h>
#include <algorithm>
#include <string>
using namespace std;

void solve() {
  int n, k, t, i, p, q, r, s, count = 0, j, flag;
  string str;
  str = "cdab";
  sort(str.begin(), str.end());
  cout << str << endl;
}

int32_t main() {
  int t;
  t = 1;
  // in t;
  while (t--) {
    solve();
  }
}