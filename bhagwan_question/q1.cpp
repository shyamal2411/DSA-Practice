#include <bits/stdc++.h>
#include <algorithm>
#include <string>
using namespace std;

#define ll long long
#define fo(i, n) for (i = 0; i < n; i++)

int main() {
  ll int n, k, t, i, p, q, r, s, count = 0, j;

  cin >> n;
  int a[n];
  fo(i, n) { cin >> a[i]; }
  fo(i, n) { cout << a[i] << " "; }
}
/*
int n;
        scanf("%d", &n);
        int a[n];
        while (n--)
        {
                scanf("%d", &a[n]);
        }
        while (n--)
        {
                printf("%d", a[n]);
        }*/