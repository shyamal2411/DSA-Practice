#include <iostream>

int main() {

    int t, n;
    cin >> t;
    for (i = 0; i < n; i++)
    {
        cin >> n;
        int arr[n];
        cin >> arr[i];
        sort(arr[i], arr[i] + n);
    }
    return 0;
}