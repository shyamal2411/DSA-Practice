#include<stdio.h>
int main()
{
int t,n,i,arr[i];
printf("Test case:");
scanf("%d\n",&t);
for(i=0;i<=t;i++)
{
  printf("Number of values to be inserted: ");
  scanf("%d\n",&n);
  for(i=0;i<=n;i++)
  {
    scanf("%d",&arr[i]);
  }
}
